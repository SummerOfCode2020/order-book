 function reconcileOrder(existingBook, incomingOrder) {
    existingBook = [{}];

    if (incomingOrder.type.value() === 'sell' ) {
        existingBook i
    }

};

let order = reconcileOrder(1,2)
console.assert(order === 'hellow', 'error')


// using console.assert for testing.
//module.exports = reconcileOrder

